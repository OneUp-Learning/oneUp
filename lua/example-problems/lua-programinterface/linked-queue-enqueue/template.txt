  //  Write a method enqueue() that adds an element the the end of the queue
  	
  	//The lasteNode should point to the new element
  	//If the queue is empty, the firstNode should be the new element
  
	public void enqueue(T newEntry) {

 [{make_answer('ans1','code-java',10,checker,10)}]

}