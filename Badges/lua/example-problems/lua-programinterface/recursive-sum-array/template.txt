Complete the recursive method sum() which returns the sum of the elements of an array .

    /**
     * compute the maximum of a range of values in an array recursively
     *
     * @param data  an array of integers
     * @param from  the first index for the sum
     * @param to    the second index for the sum
     * 
     * preconditions: from <= to, from >=0, to<length, length>0
     *                
     * @return     the sum of the elements of the array
     */
     
     public  sum(int data[], int from, int to) {

 [{make_answer('ans1','code-java',10,checker,10)}]


     }