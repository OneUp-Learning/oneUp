// Write a method equals() that compares two linked bags

	//Should check against another bag
	//Return true if both bags have the same objects
	//With the same frequencies
	
	public boolean equals(LinkedBag<T> aBag) {
	
	[{make_answer('ans1','code-java',10,checker,10)}]
	
	}